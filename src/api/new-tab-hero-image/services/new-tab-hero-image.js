'use strict';

/**
 * new-tab-hero-image service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::new-tab-hero-image.new-tab-hero-image');
