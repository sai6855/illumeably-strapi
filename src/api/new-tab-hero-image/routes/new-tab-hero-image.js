'use strict';

/**
 * new-tab-hero-image router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::new-tab-hero-image.new-tab-hero-image');
